import theme, {COLORS, SIZES, FONTS, SHADOW, Radius} from './theme';

const gender = [
  {value: 'male', label: 'male'},
  {value: 'female', label: 'female'},
];

export {theme, COLORS, SIZES, FONTS, SHADOW, gender, Radius};
